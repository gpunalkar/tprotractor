('use strict');
browser.ignoreSynchronization = true;

describe("Logout Feature", function () {

    it("Check Validation Message", function () {
        var e = browser.findElement(by.xpath("//*[]"))
    });

    it("Choose Radio Button", function () {
        browser.sleep(12000);
    });
      
    
    it("Wait", function () {
        browser.sleep(12000);
    });
});